apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: calibre
spec:
  entryPoints:
  - websecure
  routes:
    - match: Host(`{{ .Values.ingressroute.host }}`)
      kind: Rule
      services:
      - name: calibre
        port: {{ .Values.service.port }}
